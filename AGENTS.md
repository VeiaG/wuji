# Ranobes UA - Українська платформа для читання веб-новел

## Про проект

**Ranobes UA** — це сучасна веб-платформа для читання та перекладу веб-новел українською мовою. Проект побудований на основі Payload CMS 3 та Next.js 15, надаючи повнофункціональну систему управління контентом для книг, розділів, авторів, блогу та користувачів.

### Основні функції
- 📚 Каталог книг з жанрами та фільтрацією
- 📖 Читання розділів з прогресом та закладками
- ⭐ Система рейтингів та відгуків
- 💬 Коментарі до розділів
- 👤 Профілі користувачів з Google OAuth
- 📝 Блог-платформа
- 🔍 Пошук через Algolia
- 📱 Повністю адаптивний дизайн

### Технічний стек
- **Frontend:** Next.js 15 (App Router), React 19, TypeScript
- **CMS:** Payload CMS 3.0
- **Database:** MongoDB
- **Styling:** Tailwind CSS, shadcn/ui
- **Search:** Algolia
- **Auth:** Google OAuth 2.0

---

## Новий напрямок дизайну (версія 3.0)

### Концепція: "Ambient Gradients" (Оточуючі градієнти)

Головна сторінка та інші ключові сторінки використовують **динамічні градієнтні фони**, які створюються автоматично з обкладинок книг або зображень контенту.

#### Ключові принципи стилю:

1. **Повноекранні секції**
   - Кожна секція розтягується на всю ширину екрану (`w-full`)
   - Контент всередині обмежений `container mx-auto`
   - Секції безшовно з'єднані між собою з тонкими розділювачами

2. **Градієнтні фони з контенту**
   - Використання першої обкладинки секції як джерела для фонового градієнта
   - Параметри ефекту:
     - `opacity-30` — помірна прозорість для видимості
     - `blur-2xl` — сильне розмиття для створення атмосферного ефекту
     - `scale-125` — збільшення для повного покриття
   - Gradient overlay: `from-background/50 via-background/30 to-background/50`
   - Кожна секція має унікальний колір відповідно до її контенту

3. **Склеєні секції**
   - `space-y-0` між секціями (без відступів)
   - `border-b border-border/20` між секціями для тонкого візуального розділення
   - Плавний перехід градієнтів від однієї секції до іншої

4. **Структура головної сторінки**
   ```
   ┌─────────────────────────────────────────┐
   │  Останні книги        │  Коментарі      │
   ├─────────────────────────────────────────┤
   │  Набувають популярності                 │
   ├─────────────────────────────────────────┤
   │  Останні блог пости                     │
   └─────────────────────────────────────────┘
   ```

5. **Приклад реалізації**
   ```tsx
   <section className="relative overflow-hidden py-8 border-b border-border/20">
     {/* Background gradient */}
     <Image
       src={coverImage}
       className="absolute top-0 left-0 w-full h-full object-cover
                  -z-10 opacity-30 blur-2xl pointer-events-none scale-125"
     />
     <div className="absolute top-0 left-0 w-full h-full -z-10
                     bg-gradient-to-b from-background/50 via-background/30
                     to-background/50 pointer-events-none" />

     <div className="container mx-auto px-4">
       {/* Контент */}
     </div>
   </section>
   ```

6. **Застосування в інших місцях**
   - Сторінка книги вже використовує подібний підхід з `fixed` фоном
   - Планується розширення на інші сторінки (автори, жанри, профіль)

### Філософія дизайну

**Занурення в контент** — дизайн створює атмосферу кожної книги/розділу через її візуальні елементи. Користувач "відчуває" книгу ще до того, як почне читати.

**Мінімалізм + Динаміка** — чистий, мінімалістичний інтерфейс доповнюється динамічними, живими фонами, які змінюються залежно від контенту.

**Безшовність** — плавні переходи між секціями створюють відчуття єдиного цілого, а не набору окремих блоків.

---

## Версіонування

- **v2.x** — Базова функціональність, класичний card-based дизайн
- **v3.0** — Ambient Gradients дизайн, система рейтингів, покращений UX
- **v3.1+** — Планується розширення gradient дизайну на інші сторінки

---

## Для розробників

При роботі з дизайном:
- ✅ Використовуйте gradient фони для секцій з візуальним контентом
- ✅ Зберігайте консистентність параметрів (`opacity-30`, `blur-2xl`)
- ✅ Завжди додавайте gradient overlay для читабельності
- ❌ Не чіпайте компонент `BookCard` — він використовується глобально
- ❌ Не додавайте border-radius до повноекранних секцій
